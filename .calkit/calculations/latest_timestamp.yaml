name: latest_timestamp
kind: sql
engine: duckdb
environment: main
sql: select max(start_date) from 'data/activities/*.json'
deps:
  - data/activities
inputs: {}
output:
  name: latest_timestamp
  type: datetime
